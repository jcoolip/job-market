-- local dev
...\dev\job_market
.\.venv\Scripts\activate    enter venv
flask run (starts local dev test server)
git push 

-- server deploy
~/goodie/dev/job_market
lighttpd, job_market  services to run
can enter venv on server to run py scripts when ingesting \
    unless ssh tunnelling setup on dev machine.
git pull 
restart job_market service.
